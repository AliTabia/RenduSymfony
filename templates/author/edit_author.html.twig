{% extends 'base.html.twig' %}

{% block title %}Modifier l'Auteur{% endblock %}

{% block body %}
<h1>Modifier l'Auteur</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Sauvegarder</button>
{{ form_end(form) }}

<a href="{{ path('list_authors') }}" class="btn btn-secondary">Retour à la liste des auteurs</a>
{% endblock %}
